name: divtel

channels:
  - conda-forge

dependencies:
  - python>=3.10
  - astropy>=5.0
  - matplotlib>=3.6
  - numpy>=1.23
  # examples / notebooks
  - ipympl>=0.9
  - ipywidgets>=8.0
  - jupyterlab>=4.0
  # tests
  - pytest>=7.0
  - pip
  - pip:
    - -e .
